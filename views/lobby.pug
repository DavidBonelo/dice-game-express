extends layout
block content
    h1= title
    hr
    if games
        h2 Games in progress: 
        each game in games
            h4 Game id: 
                a(href="/game/" + game._id)= game._id
            p Players:
            ul
                each player in game.gamers 
                    li= player.name
    else
        h4
            a(href="/createGame") Create a new game